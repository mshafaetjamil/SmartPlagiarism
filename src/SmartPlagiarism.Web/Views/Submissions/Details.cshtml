@using SmartPlagiarism.Core.Enums
@model SmartPlagiarism.Core.DTOs.Submissions.SubmissionDetailDto
@{
    ViewData["Title"] = Model.Title;
}

<div class="container py-4">
    <nav aria-label="breadcrumb">
        <ol class="breadcrumb small">
            <li class="breadcrumb-item"><a asp-action="Index">My submissions</a></li>
            <li class="breadcrumb-item active" aria-current="page">@Model.Title</li>
        </ol>
    </nav>

    @if (TempData["StatusMessage"] is string statusMessage)
    {
        <div class="alert alert-success" role="alert">@statusMessage</div>
    }

    <div class="d-flex flex-wrap align-items-center gap-2 mb-4">
        <h1 class="h3 mb-0 me-2">@Model.Title</h1>
        <span class="badge @StatusBadge.For(Model.AnalysisStatus)">@Model.AnalysisStatus</span>
        <span class="badge @StatusBadge.For(Model.Status)">@StatusBadge.Label(Model.Status)</span>
    </div>

    <div class="row g-3">
        <div class="col-lg-4">
            <div class="card h-100 border-0 shadow-sm">
                <div class="card-header bg-transparent"><h2 class="h6 mb-0">Details</h2></div>
                <div class="card-body">
                    <dl class="row mb-0 small">
                        <dt class="col-5">Course</dt>
                        <dd class="col-7">@Model.CourseCode &ndash; @Model.CourseTitle</dd>

                        <dt class="col-5">Semester</dt>
                        <dd class="col-7">@Model.SemesterName</dd>

                        <dt class="col-5">Type</dt>
                        <dd class="col-7">@Model.Type</dd>

                        <dt class="col-5">Submitted</dt>
                        <dd class="col-7">@Model.SubmittedAtUtc.ToString("yyyy-MM-dd HH:mm") UTC</dd>
                    </dl>
                </div>
            </div>
        </div>

        <div class="col-lg-8">
            <div class="card h-100 border-0 shadow-sm">
                <div class="card-header bg-transparent"><h2 class="h6 mb-0">Originality analysis</h2></div>
                <div class="card-body">
                    @if (Model.Result is not null)
                    {
                        <div class="d-flex align-items-baseline gap-2 mb-2">
                            <span class="display-6">@Model.Result.OverallSimilarityPercent.ToString("0.00")%</span>
                            <span class="text-body-secondary">overall similarity</span>
                        </div>
                        <p class="text-body-secondary small mb-0">
                            @Model.Result.MatchCount matched source(s).
                            Analysed @Model.Result.AnalyzedAtUtc.ToString("yyyy-MM-dd HH:mm") UTC
                            by engine @Model.Result.EngineVersion.
                        </p>
                    }
                    else
                    {
                        @switch (Model.AnalysisStatus)
                        {
                            case AnalysisStatus.Queued:
                                <p class="text-body-secondary mb-0">
                                    <span class="spinner-border spinner-border-sm me-2" role="status" aria-hidden="true"></span>
                                    Analysis pending &mdash; this submission is waiting in the queue.
                                </p>
                                break;
                            case AnalysisStatus.Processing:
                                <p class="text-body-secondary mb-0">
                                    <span class="spinner-border spinner-border-sm me-2" role="status" aria-hidden="true"></span>
                                    Analysis in progress.
                                </p>
                                break;
                            case AnalysisStatus.Failed:
                                <p class="text-danger mb-0">
                                    Analysis failed. Your teacher can re-run it.
                                </p>
                                break;
                            default:
                                <p class="text-body-secondary mb-0">Analysis pending.</p>
                                break;
                        }
                    }
                </div>
            </div>
        </div>
    </div>

    <div class="card border-0 shadow-sm mt-3">
        <div class="card-header bg-transparent"><h2 class="h6 mb-0">Files</h2></div>
        <div class="table-responsive">
            <table class="table mb-0 align-middle">
                <thead>
                    <tr>
                        <th scope="col">File</th>
                        <th scope="col">Type</th>
                        <th scope="col" class="text-end">Size</th>
                        <th scope="col">SHA-256</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var file in Model.Files)
                    {
                        <tr>
                            <td>@file.OriginalFileName</td>
                            <td class="small text-body-secondary">@file.ContentType</td>
                            <td class="text-end">@StatusBadge.FormatBytes(file.SizeBytes)</td>
                            <td>
                                <code class="small" title="@file.Sha256Hash">@file.Sha256Hash[..16]&hellip;</code>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>

    @if (!string.IsNullOrWhiteSpace(Model.TeacherComment))
    {
        <div class="card border-0 shadow-sm mt-3">
            <div class="card-header bg-transparent"><h2 class="h6 mb-0">Teacher comment</h2></div>
            <div class="card-body">
                <p class="mb-0">@Model.TeacherComment</p>
            </div>
        </div>
    }
</div>
